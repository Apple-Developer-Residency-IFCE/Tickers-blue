
import SwiftUI

@main
struct TickersApp: App {
    
    var body: some Scene {
        WindowGroup {
            SoundScreenView(selectedOption: "sss")
               // .preferredColorScheme(
                    
               // )
        }
    }
}
